<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Hexo部署到阿里云</title>
      <link href="2020/10/13/hello-world/"/>
      <url>2020/10/13/hello-world/</url>
      
        <content type="html"><![CDATA[<p>##一.云服务器配置Git</p><p>1.安装依赖库</p><pre><code>yum install curl-devel expat-devel gettext-devel openssl-devel zlib-devel</code></pre><p>2.安装编译工具</p><pre><code>yum install gcc perl-ExtUtils-MakeMaker package</code></pre><p>3.查看git的版本y</p><pre><code>git version</code></pre><p>4.删除git</p><pre><code>yum remove git -y</code></pre><p>5.下载解压最新版</p><pre><code>cd /usr/local/src #下载的目录 wget https://www.kernel.org/pub/software/scm/git/git-2.28.0.tar.gz #下载最新版 tar -zxvf git-2.28.0.tar.gz #解压到当前文件夹</code></pre><p>6.编辑并安装</p><pre><code>cd git-2.28.0    #进入文件夹make prefix=/usr/local/git all    #编译源码make prefix=/usr/local/git install    #安装路径</code></pre><p>7.配置git的环境变量</p><pre><code>echo &#39;export PATH=$PATH:/usr/local/git/bin&#39; &gt;&gt; /etc/bashrc</code></pre><p>8.刷新环境变量</p><pre><code>source /etc/bashrc</code></pre><p>9.查看版本号</p><pre><code>git --version</code></pre><p>10创建git用户并且修改权限</p><pre><code>adduser qin passwd qin chmod 740 /etc/sudoers vim /etc/sudoers root ALL=(ALL) ALL fuchen ALL=(ALL) ALL</code></pre><p>11.本地windows10使用Gitbash创建密钥</p><pre><code>ssh-keygen -t rsa</code></pre><p>12.将本地创建id_rsa.pub中文件复制</p><pre><code>su fuchenmkdir ~/.sshvim ~/.ssh/authorized_keys</code></pre><p>13.本地测试</p><pre><code>ssh -v qin@服务器ip</code></pre>]]></content>
      
      
      
    </entry>
    
    
  
  
</search>
